# 运行依赖（与 pyproject.toml 的 [project].dependencies 一致，供无 uv 用户使用）
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
python-frontmatter>=1.1
rank-bm25>=0.2
faiss-cpu>=1.7
httpx>=0.27
pyyaml>=6.0

# 本地 ONNX 语义向量（离线可用，无需 PyTorch）：把 bge-small-zh-v1.5 的
# tokenizer.json + onnx/model.onnx 放进 models/bge-small-zh-v1.5/ 即可自动启用
onnxruntime>=1.17
tokenizers>=0.19

# 可选：在线 sentence-transformers 路径（联网自动下载模型；未安装时
# 有本地 ONNX 模型则用 ONNX，否则降级 FakeEmbedder，不阻塞使用）
# sentence-transformers>=2.6
